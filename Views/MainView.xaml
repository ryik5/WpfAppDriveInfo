<Window x:Class="WpfApp4.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:uc="clr-namespace:WpfApp4.Views"
        mc:Ignorable="d"
        Title="Drive Manager" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="TextBlockListItem" TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="FontFamily" Value="Verdana" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="Black" />
            </Style.Setters>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                    </MultiTrigger.Conditions>
                    <MultiTrigger.Setters>
                        <Setter Property="Background"   Value="PaleGreen" />
                    </MultiTrigger.Setters>                    
                </MultiTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ListBlockListItem" TargetType="ListBoxItem">
            <Style.Setters>
                <Setter Property="FontFamily" Value="Verdana" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="Black" />
            </Style.Setters>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="True"/>
                    </MultiTrigger.Conditions>
                    <MultiTrigger.Setters>
                        <Setter Property="Background"   Value="PaleGreen" />
                    </MultiTrigger.Setters>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="205" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ListBox Grid.Column="0" Grid.Row="1" ItemsSource="{Binding Collection}"
                 SelectedItem="{Binding SelectedDrive}" >

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Value.IsSelected}" Value="True">
                            <Setter Property="Background" Value="Red" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate  >
 
                <DataTemplate>
                    <DockPanel LastChildFill="True">
                        <TextBlock  Style="{StaticResource TextBlockListItem}" Width="190">
                            <Run FontSize="18" Text="{Binding   Path=Name, StringFormat=Drive ({0}) }" />
                            <LineBreak/>
                            <Run Text="{Binding   Path=Type, StringFormat= ({0}) }"  />
                            <LineBreak/>
                            <Run Text="{Binding   Path=FileSystem, StringFormat=Drive ({0}) }"/>
                        </TextBlock>
                        
                        <!--TextBlock  Text="{Binding Path=FileSystem}" Style="{StaticResource TextBlockListItem}"/-->
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="0" Grid.Row="0" DataContext="{Binding SelectedDrive}">
            <TextBlock  Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Column="1" Grid.Row="1" DataContext="{Binding SelectedDrive}">
            <TextBlock Text="Выбранный элемент"  />
            <TextBlock Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</Window>
